apiVersion: v1
kind: Service
metadata:
  name: bge-m3-ollama
  namespace: kubeai
spec:
  selector:
    model: bge-m3
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bge-m3-ollama
  namespace: kubeai
  annotations:
    konghq.com/strip-path: "true"
spec:
  ingressClassName: kong
  rules:
    - http:
        paths:
          - path: /bge-m3
            pathType: Prefix
            backend:
              service:
                name: bge-m3-ollama
                port:
                  number: 8000
