i1. Deploy Strimzi using installation files

kubectl create namespace kafka
kubectl create -f 'https://strimzi.io/install/latest?namespace=kafka' -n kafka

2. Create an Apache Kafka cluster
# Apply the `Kafka` Cluster CR file
kubectl apply -f https://strimzi.io/examples/latest/kafka/kafka-single-node.yaml -n kafka

3. producer pod 생성
kubectl create -n kafka deployment kafka-producer \
  --image=quay.io/strimzi/kafka:0.49.1-kafka-4.1.1 \
  -- sleep infinity
4. consumer pod 생성
kubectl create -n kafka deployment kafka-consumer \
  --image=quay.io/strimzi/kafka:0.49.1-kafka-4.1.1 \
  -- sleep infinity

5. Producer에서 메시지 전송
# Pod에 접속
kubectl exec -n kafka -it deployment/kafka-producer -- bash

# Shell 내부에서 실행
bin/kafka-console-producer.sh \
  --bootstrap-server my-cluster-kafka-bootstrap:9092 \
  --topic my-topic

6. Consumer에서 메시지 수신
# Pod에 접속
kubectl exec -n kafka -it deployment/kafka-consumer -- bash

# Shell 내부에서 실행
bin/kafka-console-consumer.sh \
  --bootstrap-server my-cluster-kafka-bootstrap:9092 \
  --topic my-topic \
  --from-beginning


7. kafka ui 실행
helm install kafka-ui /app/mykubernetes/kafka/kafka-ui -n kafka

8. wsl에서는 kafbat-ui에 firefox를 ubuntu 내부에 설치하고
   192로컬 ip로 접근한다

* metallb 세팅
➜ minikube ip
192.168.49.2

➜ minikube addons configure metallb
-- Enter Load Balancer Start IP: 192.168.49.100
-- Enter Load Balancer End IP: 192.168.49.150
    ▪ Using image quay.io/metallb/speaker:v0.9.6
    ▪ Using image quay.io/metallb/controller:v0.9.6
✅  metallb was successfully configured   

9. kafbat-ui를 실행하면 producer 테스트를 GUI에서 직접 수행할 수 있음

topics -> my-topic -> produce message -> value에 "hellow world"
