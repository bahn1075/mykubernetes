apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

resources:
  - ingress.yaml
  - pv-pvc.yaml

images:
  - name: jenkins/jenkins
    newName: index.docker.io/jenkins/jenkins
    newTag: lts-jdk21

patchesStrategicMerge:
  - deployment-patch.yaml
